# 张旭个人站 · 部署说明书

> 更新时间：2026-08-14
> 适用项目：senlin_web-3D 克隆（Vite + React + Three.js 3D 个人站）

## 一、这份网站是怎么部署的（先看这一节）

- **当前线上地址**：https://zhaosenlin-bit.github.io/zhangxu____web/
- **托管平台**：GitHub Pages（免费静态托管，用 GitHub 账号 `zhaosenlin-bit` 发布）
- **发布方式**：把打包好的网站推到仓库的 `gh-pages` 分支，GitHub 自动发布到 `github.io` 域名
- **原理**：项目是静态网站。先用 Vite 把代码「打包」成纯网页文件，再推到 `gh-pages` 分支，线上自动生效。

```
本地代码 → 把路径前缀 /cartoon/ 改成 /zhangxu____web/ → npm run build 打包 → 推到 gh-pages 分支 → 线上生效
```

## 二、本地开发（改完先在自己电脑上看效果）

### 1. 首次准备（只需要做一次）
先安装依赖：

```
npm.cmd install
```

### 2. 启动本地网站
双击项目里的 `启动网站.bat`，或者手动运行：

```
npm.cmd run dev
```

然后浏览器打开：http://localhost:5173/cartoon/

### 3. 日常开发
- 改完代码保存，浏览器会自动刷新（热更新），不需要重启
- 关掉黑色命令行窗口 = 关闭本地网站

### 4. 本地常见问题
| 现象 | 原因 | 解决办法 |
| --- | --- | --- |
| 打不开，显示 ERR_CONNECTION_REFUSED | 本地服务器没在运行 | 重新双击 `启动网站.bat` |
| 一刷新就变白屏 | 服务器停了 | 重新启动即可 |
| 提示「禁止运行脚本」 | PowerShell 脚本策略限制 | 用 `npm.cmd`，不要用 `npm` |

## 三、正式发布（让网站上线）

### 方式 A：GitHub Pages（当前实际使用，推荐）
1. **改路径前缀**：把 `vite.config.js` 里的 `base: '/cartoon/'` 改成 `base: '/zhangxu____web/'`，并把 `src/` 下所有 `/cartoon/` 文字替换成 `/zhangxu____web/`（这类替换也可以直接让 Codex 代做）
2. **构建**：
   ```
   npm.cmd run build
   ```
3. **推送 `gh-pages` 分支**：把 `dist/cartoon` 里的内容覆盖到 `gh-pages` 分支（同时把 `index.html` 复制一份为 `404.html`，用于路由兜底）
4. **等 1-5 分钟**，访问：https://zhaosenlin-bit.github.io/zhangxu____web/

### 方式 B：Cloudflare Pages（备用，需要 Cloudflare 账号）
- 旧版曾部署在 https://senlin-c1n.pages.dev/cartoon/，该项目是「手动直传」模式，推送 GitHub 不会自动更新
- 需要登录能管理 `senlin-c1n` 项目的 Cloudflare 账号，在 Workers & Pages 里上传 `dist` 内容（或使用 wrangler 命令行）
- 注意：Cloudflare Pages 单文件不能超过 25MB，视频需先压缩

## 四、日常更新流程（改完内容怎么上线）

1. 改内容：文字、联系方式、图片路径大多集中在 `src/config/userContent.js`
2. 本地看效果：双击 `启动网站.bat` → 打开 http://localhost:5173/cartoon/
3. 确认没问题后发布：按「方式 A」重新构建并推送 `gh-pages` 分支，等 1-5 分钟线上生效

## 五、域名说明

- 当前域名：https://zhaosenlin-bit.github.io/zhangxu____web/（GitHub Pages 免费域名）
- 注意：线上访问路径不带 `/cartoon/`，因为部署时把前缀改成了 `/zhangxu____web/`
- 以后想用自己的域名：GitHub 仓库 Settings → Pages → Custom domain 绑定

## 六、关键文件说明

| 文件 | 作用 |
| --- | --- |
| `vite.config.js` | 打包配置：`base` 决定线上路径前缀（`/cartoon/` 或 `/zhangxu____web/`） |
| `src/` 里的 `/cartoon/` 路径 | 资源路径前缀，部署到 GitHub Pages 时要统一替换成 `/zhangxu____web/` |
| `404.html` | GitHub Pages 路由兜底：访问任意子路径都返回首页 |
| `public/_redirects` | 仅供 Cloudflare 使用（GitHub Pages 不读取） |
| `src/config/userContent.js` | 全站文字、联系方式、图片路径的集中位置 |
| `启动网站.bat` | 一键启动本地开发服务器 |

## 七、常见问题（FAQ）

**Q1：改了代码并推送了 main 分支，为什么线上没变化？**
- 线上只认 `gh-pages` 分支；改完要按「方式 A」重新构建并推 `gh-pages`

**Q2：线上页面打不开/白屏？**
- 先访问 https://zhaosenlin-bit.github.io/zhangxu____web/ 确认主站正常；如果本地打不开，先双击 `启动网站.bat`

**Q3：视频/图片太大的话会怎样？**
- Cloudflare Pages 单文件上限 25MB；GitHub Pages 也有大小限制。建议视频压到 25MB 以内（本项目的黑板视频已压到约 1.5MB）

**Q4：构建报错怎么办？**
- 把报错截图发给 Codex，改完重新 `npm run build` 即可

## 八、当前状态（2026-08-14 核对）

- 线上站点：https://zhaosenlin-bit.github.io/zhangxu____web/ = 张旭版，已包含电话（13872498847）、哔哩哔哩（坐着鼠鼠环游世界）、QQ 联系方式
- 黑板视频已压缩至约 1.5MB；页面、图片、二维码、404 兜底均已验证正常
