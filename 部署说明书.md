# 张旭个人站 · 部署说明书

> 更新时间：2026-08-13
> 适用项目：senlin_web-3D 克隆（Vite + React + Three.js 3D 个人站）

## 一、这份网站是怎么部署的（先看这一节）

- **托管平台**：Cloudflare Pages（免费静态托管，自带 HTTPS 和全球加速）
- **线上地址**：https://senlin-c1n.pages.dev/cartoon/
- **代码仓库**：GitHub `zhaosenlin-bit/zhangxu____web`（分支 `main`）
- **原理**：项目是静态网站。先用 Vite 把代码「打包」成纯网页文件（放在 `dist` 文件夹），再把 `dist` 里的文件发布到 Cloudflare Pages，全世界都能通过网址访问。

```
本地代码 → npm run build 打包 → dist 文件夹 → Cloudflare Pages → 线上网址
```

## 二、本地开发（改完先在自己电脑上看效果）

### 1. 首次准备（只需要做一次）
先安装依赖：

```
npm.cmd install
```

### 2. 启动本地网站
双击项目里的 `启动网站.bat`，或者手动运行：

```
npm.cmd run dev
```

然后浏览器打开：http://localhost:5173/cartoon/

### 3. 日常开发
- 改完代码保存，浏览器会自动刷新（热更新），不需要重启
- 关掉黑色命令行窗口 = 关闭本地网站

### 4. 本地常见问题
| 现象 | 原因 | 解决办法 |
| --- | --- | --- |
| 打不开，显示 ERR_CONNECTION_REFUSED | 本地服务器没在运行 | 重新双击 `启动网站.bat` |
| 一刷新就变白屏 | 服务器停了 | 重新启动即可 |
| 提示「禁止运行脚本」 | PowerShell 脚本策略限制 | 用 `npm.cmd`，不要用 `npm` |

## 三、正式发布（让网站上线）—— 两种方式，推荐方式 A

### 方式 A：连接 GitHub 自动部署（推荐）
以后只要把代码推到 GitHub，Cloudflare 会自动重新构建和发布，不用手动上传。

1. **把代码推到 GitHub**（本机已配好远程仓库 `zhangxu`）：
   ```
   git add .
   git commit -m "更新网站内容"
   git push zhangxu main
   ```
2. **登录 Cloudflare**：打开 https://dash.cloudflare.com
3. 进入 **Workers & Pages → Create → Pages → Connect to Git**
4. 选择 GitHub 仓库 `zhangxu____web`
5. **构建设置填这 3 项**（其他保持默认）：
   - Framework preset：`None`
   - Build command：`npm run build`
   - Build output directory：`dist`
6. 点击 **Save and Deploy**，等 1-3 分钟构建完成
7. 完成后访问：https://senlin-c1n.pages.dev/cartoon/

以后每次更新：改代码 → `git push zhangxu main` → 等 2-3 分钟 → 线上自动更新。

### 方式 B：手动上传 dist（不依赖 GitHub）
1. 本地打包：
   ```
   npm.cmd run build
   ```
2. 打包产物在项目 `dist` 文件夹（里面有 index.html、assets 等）
3. 打开 Cloudflare Pages → **Create → Direct Upload**
4. 把 `dist` 文件夹里的**全部内容**拖上去上传
5. 上传完自动发布，访问 https://senlin-c1n.pages.dev/cartoon/

## 四、日常更新流程（改完内容怎么上线）

1. 改内容：文字、联系方式、图片路径大多集中在 `src/config/userContent.js`
2. 本地看效果：双击 `启动网站.bat` → 打开 http://localhost:5173/cartoon/
3. 确认没问题后发布：
   - **方式 A**：`git push zhangxu main`，等自动构建完成
   - **方式 B**：`npm.cmd run build` → 重新上传 `dist`

## 五、域名说明

- 免费域名：`https://senlin-c1n.pages.dev`，访问路径固定带 `/cartoon/`（打包配置里 `base: '/cartoon/'`）
- 以后想用自己的域名：Cloudflare Pages → Custom domains → 添加域名（域名需托管在 Cloudflare，免费）

## 六、关键文件说明

| 文件 | 作用 |
| --- | --- |
| `vite.config.js` | 打包配置：`base='/cartoon/'`，输出到 `dist/cartoon` |
| `public/_redirects` | 路由回退：访问 `/cartoon/about` 等页面时返回 `index.html` |
| `public/_headers` | 缓存策略：HTML 不缓存，图片/字体/纹理长期缓存 |
| `src/config/userContent.js` | 全站文字、联系方式、图片路径的集中位置 |
| `启动网站.bat` | 一键启动本地开发服务器 |

## 七、常见问题（FAQ）

**Q1：线上改了内容但没变化？**
- 方式 A：确认已 `git push`，等构建完成（2-3 分钟），再按 Ctrl+F5 强制刷新
- 方式 B：重新 `npm run build` 并重新上传

**Q2：线上打开某个页面白屏？**
- 检查 `public/_redirects` 里有没有对应路由；先访问 https://senlin-c1n.pages.dev/cartoon/ 确认主站正常

**Q3：`dist` 里有没有 `_headers` / `_redirects`？**
- 有，构建时会自动从 `public/` 复制到 `dist`，Cloudflare Pages 会自动读取生效

**Q4：构建报错怎么办？**
- 把报错截图发给 Codex，改完重新 `npm run build` 即可

## 八、当前状态（2026-08-13 核对）

- 本地代码：张旭版（标题「张旭 · 个人站」），已包含电话、哔哩哔哩、QQ 联系方式
- 线上站点：https://senlin-c1n.pages.dev/cartoon/ 目前还是旧版「森林 · Senlin Studio」
- 如需把张旭版上线：按「四、日常更新流程」推送或上传一次即可
