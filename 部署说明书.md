# 张旭个人站 · 部署说明书

> 更新时间：2026-08-14
> 适用项目：zhangxu____web（Vite + React + Three.js 3D 个人站）

## 一、网站部署在哪

- **线上地址（GitHub Pages）**：https://zhaosenlin-bit.github.io/zhangxu____web/
  - 主站：`https://zhaosenlin-bit.github.io/zhangxu____web/`
  - 词域探险小游戏：`https://zhaosenlin-bit.github.io/zhangxu____web/vocab/`
- **托管平台**：GitHub Pages（免费静态托管，用 GitHub 账号 `zhaosenlin-bit` 发布）
- **代码仓库**：GitHub `zhaosenlin-bit/zhangxu____web`（分支 `main`）
- **发布分支**：`gh-pages`（由部署脚本自动生成并推送，不要手动改它）
- **原理**：本地代码 → Vite 打包（base=/zhangxu____web/）→ 产物推到 `gh-pages` 分支 → GitHub 自动发布

```
本地代码 → npm run build → gh-pages 分支 → 线上网址
```

## 二、日常更新发布（重点）

1. 改完内容（大多集中在 `src/config/userContent.js`）
2. 双击项目根目录的 **`部署到GitHubPages.bat`**
   （等价于命令行执行：`node scripts/deploy-github-pages.mjs`）
3. 脚本会自动完成：构建 → 路径重写 → 推送到 `gh-pages`
4. 等 1-2 分钟，打开线上地址，按 Ctrl+F5 强制刷新

> 推送时使用保存在本机的 GitHub 账号（zhaosenlin-bit）凭据，不需要登录任何网页。
> 只推送 `main` 分支不会更新线上，线上只认 `gh-pages` 分支。

## 三、本地开发

1. 首次准备：`npm.cmd install`
2. 启动本地网站：双击 `启动网站.bat`，或运行 `npm.cmd run dev`
3. 浏览器打开：http://localhost:5173/cartoon/
4. 改完代码保存，浏览器自动刷新；关掉命令行窗口 = 关闭本地网站

常见问题：

| 现象 | 原因 | 解决办法 |
| --- | --- | --- |
| 打不开，显示 ERR_CONNECTION_REFUSED | 本地服务器没在运行 | 重新双击 `启动网站.bat` |
| 一刷新就变白屏 | 服务器停了 | 重新启动即可 |
| 提示「禁止运行脚本」 | PowerShell 脚本策略限制 | 用 `npm.cmd`，不要用 `npm` |

## 四、部署脚本做了什么（了解即可）

- 用 `--base=/zhangxu____web/` 重新构建（Vite 资源地址指向 GitHub Pages 路径）
- 把源码里硬编码的 `/cartoon/...` 资源路径重写为 `/zhangxu____web/...`
- 添加 `404.html`（SPA 深链回退）和 `.nojekyll`（跳过 Jekyll 处理）
- 把产物推送到 `gh-pages` 分支（强制覆盖）

## 五、Cloudflare（备用，暂未启用）

- 旧版曾部署在 `https://senlin-c1n.pages.dev/cartoon/`，那个项目是「手动直传」模式，推送 GitHub 不会自动更新，目前线上仍是旧版「森林 · Senlin Studio」，本次未改动。
- 如果以后愿意登录 Cloudflare：Workers & Pages → Create → Pages → Connect to Git，构建设置用 Framework preset=`None`、Build command=`npm run build`、Build output directory=`dist`。
- 注意：Cloudflare Pages 单文件不能超过 25MB（本项目的黑板视频已压到约 1.5MB）。

## 六、域名说明

- 免费域名：`https://zhaosenlin-bit.github.io/zhangxu____web/`（GitHub Pages）
- 注意：线上访问路径不带 `/cartoon/`，部署脚本会把前缀统一成 `/zhangxu____web/`
- 以后想用自己的域名：GitHub 仓库 Settings → Pages → Custom domain 绑定

## 七、关键文件

| 文件 | 作用 |
| --- | --- |
| `vite.config.js` | 打包配置：base=`/cartoon/`（开发/Cloudflare），输出 `dist/cartoon` |
| `scripts/deploy-github-pages.mjs` | 一键部署脚本（构建 + 路径重写 + 推送 gh-pages） |
| `部署到GitHubPages.bat` | 双击运行部署脚本 |
| `public/_redirects`、`public/_headers` | 仅供 Cloudflare 使用（GitHub Pages 不读取） |
| `src/config/userContent.js` | 全站文字、联系方式、图片路径的集中位置 |
| `src/hooks/useDocumentMeta.js` | 路由基准 BASE 从构建配置自动推导（两种部署都兼容） |

## 八、当前状态（2026-08-14 核对）

- 线上（GitHub Pages）：张旭版最新提交 `372a432`，含词域探险游戏、黑板视频、全部贴图/字体/音效；电话（13872498847）、哔哩哔哩（坐着鼠鼠环游世界）、QQ 联系方式均已验证
- 线上（Cloudflare `senlin-c1n.pages.dev/cartoon/`）：仍是旧版「森林 · Senlin Studio」，未改动

## 九、常见问题（FAQ）

**Q1：改了代码并推送了 main 分支，为什么线上没变化？**
- 线上只认 `gh-pages` 分支；改完要按「二、日常更新发布」运行部署脚本

**Q2：线上页面打不开/白屏？**
- 先访问 https://zhaosenlin-bit.github.io/zhangxu____web/ 确认主站正常；如果本地打不开，先双击 `启动网站.bat`

**Q3：视频/图片太大了会怎样？**
- GitHub Pages 和 Cloudflare 都有文件大小限制，建议视频压到 25MB 以内（本项目的黑板视频已压到约 1.5MB）

**Q4：构建报错怎么办？**
- 把报错截图发给 Codex，改完重新 `npm run build` 即可